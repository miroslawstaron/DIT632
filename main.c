#include <stdio.h>
#include <stdlib.h>

// This is the first program
//and the use of the pointers and memory allocation
void main(void)
{
	for (int i= 0; i<10; i++)
		printf("Hello number %d\n", i);
}
